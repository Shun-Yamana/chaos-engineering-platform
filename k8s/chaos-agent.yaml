# FIS_TEMPLATE_* は deploy.yml の sed で GitHub Secrets から注入される
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: chaos-agent
  namespace: chaos
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::203553641035:role/chaos-agent-role
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: chaos-agent
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "delete"]
  - apiGroups: [""]
    resources: ["configmaps"]
    verbs: ["get", "patch", "update"]
  - apiGroups: ["apps"]
    resources: ["deployments"]
    verbs: ["get", "patch", "update"]
  - apiGroups: ["apps"]
    resources: ["deployments/scale"]
    verbs: ["patch"]
  - apiGroups: ["networking.k8s.io"]
    resources: ["ingresses"]
    verbs: ["get"]
  # Chaos Mesh CRD 管理 (ADR 079, ADR 081)
  - apiGroups: ["chaos-mesh.org"]
    resources: ["podchaoses", "stresschaoses", "networkchaoses", "httpchaoses"]
    verbs: ["create", "get", "delete", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: chaos-agent
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: chaos-agent
subjects:
  - kind: ServiceAccount
    name: chaos-agent
    namespace: chaos
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: chaos-agent
  namespace: chaos
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: chaos-agent
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: chaos-agent
    spec:
      serviceAccountName: chaos-agent
      automountServiceAccountToken: true
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      volumes:
        - name: tmp
          emptyDir: {}
      containers:
        - name: chaos-agent
          image: CHAOS_AGENT_IMAGE
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          livenessProbe:
            exec:
              command: ["python", "-c", "import os; os.kill(1, 0)"]
            initialDelaySeconds: 10
            periodSeconds: 30
            failureThreshold: 3
          env:
            - name: TABLE_NAME
              value: chaos-platform-experiment-history
            - name: AWS_REGION
              value: ap-northeast-1
            # node_failure 実験用 FIS テンプレート (ADR 079, ADR 080)
            - name: FIS_TEMPLATE_NODE_FAILURE
              value: PLACEHOLDER_FIS_TEMPLATE_NODE_FAILURE
            # az_isolation 実験用 FIS テンプレート (ADR 083)
            - name: FIS_TEMPLATE_AZ_ISOLATION
              value: PLACEHOLDER_FIS_TEMPLATE_AZ_ISOLATION
          resources:
            requests:
              cpu: "64m"
              memory: "128Mi"
            limits:
              cpu: "256m"
              memory: "256Mi"
