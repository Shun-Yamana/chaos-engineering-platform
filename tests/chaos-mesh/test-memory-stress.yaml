apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: test-memory-stress
  namespace: test
spec:
  mode: one
  selector:
    namespaces:
      - test
    labelSelectors:
      app: nginx-target
  stressors:
    memory:
      workers: 1
      size: 64MB
  duration: 30s
