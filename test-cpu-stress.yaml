apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: test-cpu-stress
  namespace: test
spec:
  mode: one
  selector:
    namespaces:
      - test
    labelSelectors:
      app: target
  stressors:
    cpu:
      workers: 2
      load: 80
  duration: 30s
